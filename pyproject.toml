[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "soil-course"
version = "0.1.0"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "pandas",
    "openpyxl",
    "statsmodels"
    "ipywidgets",
    "rosetta-soil"
]
